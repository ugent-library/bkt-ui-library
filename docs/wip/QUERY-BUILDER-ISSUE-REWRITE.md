# Query builder issue rewrite — draft

The filed issues predate the phase-1 revision. The rewritten bodies live in the local
`ISSUE-*` files — 00, 02, 03, 04, 05, and 08 for the phase-2 placeholder #295; at
filing time M copies each body over its GitHub issue. This file lists what changed.
File 00–05 after the reveal lands, with re-captured screenshots; 08 waits for
phase 2.

## #223 epic (ISSUE-00)

The epic now says the work is front-end — backend gaps become backend issues or
stubs. Phasing: the backoffice field set is phase 2, fields TBD (#295); OR groups are
phase 3 (#230). Marie Curator arrives with phase 2; Quinn Query stays out (API). Free
text never becomes a condition (the bet's no-go). Acceptance matches the golden-set
gate: authorable, or signed off (not exposed, over cap, parked for a later phase).

## #225 (ISSUE-02)

The chooser offers 14 public fields, down from 21. The Licence public-exposure dependency is deleted — the
field is backoffice-only. The OR-suggestion sentence is deleted — the combine offer
is removed, never building; the "Add an 'or'" action is gated out of phase-1 views.

## #226 (ISSUE-03)

The identifier paste box and its 763-value evidence move to #295. The multi-value
sentence is rewritten. The five-value cap with reported drops replaces the
Save-search pointer and "several hundred values remains usable". Open-ended year
bounds are added. Title and Keywords autocomplete becomes a raven dependency.

## #227 (ISSUE-04)

The zero-result-help question is deleted: spelling feedback is out of scope, the OR
suggestion is dropped. The count-wording question stays.

## #228 (ISSUE-05)

The search box's free text is out of scope — never a condition (the bet's no-go). The
sidebar's condition-held tile is out too — the filter chips are the page's only
reflection of builder conditions; its state stays future design. Quinn Query's share sentence is gone; the address decision joins the
dependencies.

## #224, #229, #230

No change; the backoffice hosts belong to #295, and the epic carries #230's phase-3
label.

## Handoff note

At handoff the field contract is promoted out of `docs/wip/`, not deleted.
