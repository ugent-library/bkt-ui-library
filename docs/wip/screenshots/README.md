# Work-card issue screenshots

Element screenshots from the running kit (`npm start`, viewport 1600, 2× scale),
cropped to the region each issue's `> **Screenshot:**` line names. The filename
carries the issue numbers that reference it; a file listed twice is one shot two
issues share.

| Issue | Screenshot line | File |
|---|---|---|
| **00-EPIC-public** | the works feed (`public-works.html`) — one card per work type | `epic-public-02--works-feed-1.png`, `-2`, `-3` |
| **00-EPIC-backoffice** | the curator list (`curate.html`) and the researcher's own list (`search-researcher.html`) | `epic-backoffice-04-06--curate-list.png`, `epic-backoffice-04-06--search-researcher-list.png` |
| **01** card grammar | the grammar section of the kit page (`patterns/work-card.html`) | `01--work-card-grammar.png` |
| **02** public reference line | the works feed with one card per work type (`public-works.html`) | `epic-public-02--works-feed-1.png`, `-2`, `-3` |
| **03** access status | the four states on the kit page (`patterns/work-card.html`, the `__meta` demo) | `03--work-card-meta-states.png` |
| **03** access status | a feed card with no access badge (a conference presentation in `public-works.html`) | `03--public-works-no-access-badge.png` |
| **04** backoffice card | the curator list (`curate.html`) — four statuses down one column | `epic-backoffice-04-06--curate-list.png` |
| **04** backoffice card | the researcher's own list (`search-researcher.html`) with a draft and a returned record | `epic-backoffice-04-06--search-researcher-list.png` |
| **05** card messages | the researcher's view, warning block only (`search-researcher.html`, the draft dataset) | `05--search-researcher-draft-dataset.png` |
| **05** card messages | a curator's view of the same kind of card, both blocks (`curate.html`, first card) | `05--curate-first-card.png` |
| **06** card actions | the five rows — curator submitted, curator returned, researcher draft, researcher returned, researcher reviewed | `epic-backoffice-04-06--curate-list.png`, `epic-backoffice-04-06--search-researcher-list.png` |
| **07** match card amendment | the related research panel on the work detail page (`public-work-detail.html`) | `07--work-detail-related.png` |
| **12** retracted badge | the retracted public card (`patterns/work-card.html`, "Public card — retracted") | `12--work-card-public-retracted.png` |
| **12** retracted badge | a retracted record in the researcher's list (`search-researcher.html`) | `12--search-researcher-retracted.png` |

**08, 09, 10, 11** are backend issues and say "No screenshot".
